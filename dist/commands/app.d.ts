/**
 * Manage "apps" voicelogger pushes logs into (a first cut of the BRAINSTORM
 * `--app` idea). Registry lives at ~/.voicelogger/apps.json.
 */
export declare function appCommand(args: string[]): Promise<void>;
